#include "holberton.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * funcion - 
 * @variable: 
 * @variable: 
 * Return: 
*/

int main(int argc, int argv[])
{
    if(argc != 3)
    {
        printf("Error\n");
        exit(98);
    }
    if(sizeof(argv[1]) < 4)
    {
        printf("Error\n");
        exit(98);
    }

 return (str);
}