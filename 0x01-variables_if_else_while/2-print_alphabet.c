#include <stdio.h>
/**
 * main - first function to execute when run program
 * print alphabet lower case
 * Return: 0
 */
int main(void)
{
char n;

for (n = 'a' ; n <= 'z' ; n++)
putchar(n);
putchar('\n');
return (0);
}
